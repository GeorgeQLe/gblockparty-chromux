# Post-Sync Actions

## Dependencies

```sh
npm --prefix prototype install
```

## Notifications

- `prototype/package.json` — review dependency or app-script changes
- `prototype/package-lock.json` — dependencies changed
- `CLAUDE.md` — review project conventions
- `AGENTS.md` — review agent instructions
